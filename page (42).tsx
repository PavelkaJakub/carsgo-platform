import Nav from "@/components/Nav";import {requirePartner} from "@/lib/auth";import {prisma} from "@/lib/prisma";import {redirect} from "next/navigation";
export default async function P(){let s;try{s=await requirePartner()}catch{redirect("/login")}if(!s.companyId)redirect("/login");const vs=await prisma.vehicle.findMany({where:{companyId:s.companyId},include:{images:true},orderBy:{createdAt:"desc"}});
return <><Nav/><main className="container section"><div style={{display:"flex",justifyContent:"space-between"}}><h1>Vozidla</h1><a className="btn" href="/partner/vehicles/new">Přidat</a></div><table className="table"><thead><tr><th>Vozidlo</th><th>Cena</th><th>Stav</th></tr></thead><tbody>{vs.map(v=><tr key={v.id}><td>{v.brand} {v.model}</td><td>{v.price.toLocaleString("cs-CZ")} Kč</td><td>{v.status}</td></tr>)}</tbody></table></main></>}
